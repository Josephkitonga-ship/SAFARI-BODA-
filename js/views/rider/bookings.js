/* ============================================
   SAFARI BODA — RIDER BOOKINGS VIEW
   Stub — lists bookings assigned to the signed-in
   rider, filtered by Supabase row-level security.
   ============================================ */

SafariBoda.views.rider.bookings = function () {
  document.getElementById('app').innerHTML = `
    <div class="container sb-section">
      <h1 class="sb-section-title">My bookings</h1>
      <p class="field-hint">Booking list will appear here once the schema is wired in.</p>
    </div>
  `;
};
