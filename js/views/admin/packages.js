/* ============================================
   SAFARI BODA — ADMIN PACKAGES VIEW
   Stub — manage package pricing/content (currently
   Basic/Standard/Premium at 3,000/6,000/9,000 KES).
   ============================================ */

SafariBoda.views.admin.packages = function () {
  document.getElementById('app').innerHTML = `
    <div class="container sb-section">
      <h1 class="sb-section-title">Packages</h1>
      <p class="field-hint">Package pricing management will appear here once the schema is wired in.</p>
    </div>
  `;
};
