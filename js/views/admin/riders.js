/* ============================================
   SAFARI BODA — ADMIN RIDERS VIEW
   Stub — create/activate rider accounts (admin-only,
   no self-registration per Foundations Report §5).
   ============================================ */

SafariBoda.views.admin.riders = function () {
  document.getElementById('app').innerHTML = `
    <div class="container sb-section">
      <h1 class="sb-section-title">Riders</h1>
      <p class="field-hint">Rider management (create/activate accounts) will appear here once the schema is wired in.</p>
    </div>
  `;
};
