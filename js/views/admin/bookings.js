/* ============================================
   SAFARI BODA — ADMIN BOOKINGS VIEW
   Stub — manual booking-to-rider assignment
   (Foundations Report §6).
   ============================================ */

SafariBoda.views.admin.bookings = function () {
  document.getElementById('app').innerHTML = `
    <div class="container sb-section">
      <h1 class="sb-section-title">Bookings</h1>
      <p class="field-hint">Booking list + rider assignment will appear here once the schema is wired in.</p>
    </div>
  `;
};
